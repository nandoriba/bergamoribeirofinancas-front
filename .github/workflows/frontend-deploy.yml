name: frontend-ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: frontend-deploy-production
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/bergamoribeirofinancas-frontend
  IMAGE_TAG: ${{ github.sha }}
  VITE_API_BASE_URL_PRODUCTION: ${{ secrets.VITE_API_BASE_URL_PRODUCTION || vars.VITE_API_BASE_URL_PRODUCTION }}

jobs:
  test:
    name: Lint, typecheck e testes
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Tests
        run: npm run test

  build-and-push:
    name: Build e push da imagem
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref_name == 'main'
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validar VITE_API_BASE_URL_PRODUCTION
        run: |
          if [ -z "$VITE_API_BASE_URL_PRODUCTION" ]; then
            echo "VITE_API_BASE_URL_PRODUCTION deve estar configurada como secret ou variable do repositório."
            exit 1
          fi

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login no GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Lowercase owner
        id: meta
        shell: bash
        run: |
          OWNER="$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')"
          echo "image=ghcr.io/${OWNER}/bergamoribeirofinancas-frontend" >> "$GITHUB_OUTPUT"

      - name: Build e push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ steps.meta.outputs.image }}:${{ env.IMAGE_TAG }}
            ${{ steps.meta.outputs.image }}:latest
          build-args: |
            VITE_API_BASE_URL=${{ env.VITE_API_BASE_URL_PRODUCTION }}

  deploy:
    name: Deploy na VPS
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.event_name == 'push' && github.ref_name == 'main'
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        env:
          FRONTEND_TAG: ${{ github.sha }}
          GHCR_USERNAME: ${{ secrets.GHCR_USERNAME }}
          GHCR_PAT: ${{ secrets.GHCR_PAT }}
          VPS_APP_DIR: ${{ secrets.VPS_APP_DIR }}
        with:
          host: ${{ secrets.VPS_HOST }}
          port: ${{ secrets.VPS_PORT || 22 }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script_stop: true
          envs: FRONTEND_TAG,GHCR_USERNAME,GHCR_PAT,VPS_APP_DIR
          script: |
            set -eu
            if [ "$VPS_APP_DIR" != "/app/financeiro" ]; then
              echo "VPS_APP_DIR deve ser /app/financeiro para não sobrescrever a stack do Psicocomportamento."
              exit 1
            fi
            cd "$VPS_APP_DIR"
            if [ ! -f .env ]; then
              echo "Arquivo $VPS_APP_DIR/.env não encontrado. Provisione o .env de produção antes do deploy."
              exit 1
            fi
            if [ ! -f docker-compose.yml ]; then
              echo "Arquivo $VPS_APP_DIR/docker-compose.yml não encontrado. Rode o deploy do backend primeiro para publicar o compose."
              exit 1
            fi
            docker network inspect app_internal > /dev/null
            echo "$GHCR_PAT" | docker login ghcr.io -u "$GHCR_USERNAME" --password-stdin
            if grep -q '^FRONTEND_TAG=' .env; then
              sed -i "s/^FRONTEND_TAG=.*/FRONTEND_TAG=${FRONTEND_TAG}/" .env
            else
              printf '\nFRONTEND_TAG=%s\n' "${FRONTEND_TAG}" >> .env
            fi
            docker compose -p financeiro -f "$VPS_APP_DIR/docker-compose.yml" pull frontend
            docker compose -p financeiro -f "$VPS_APP_DIR/docker-compose.yml" up -d frontend
            sleep 5
            curl -fsS http://127.0.0.1:8181 > /dev/null
            docker image prune -f
